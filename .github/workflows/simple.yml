name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: Ubuntu-latest
    steps:
      - name: eccho a String
        run: echo "Hello World"
      - name: multiline script
        run: |
          node -v
          npm -v